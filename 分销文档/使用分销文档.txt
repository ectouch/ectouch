1、 将安装文件中的文件，按照目录结构移动到pc端后台。
2、执行sql
	ALTER TABLE `ecs_users` ADD COLUMN `apply_sale` int(1) UNSIGNED NOT NULL DEFAULT 0;
	ALTER TABLE `ecs_users` ADD COLUMN `apply_time` int(11) UNSIGNED NOT NULL DEFAULT 0;
	
	INSERT INTO `ecs_user_rank` VALUES ('100', '分销用户', '0', '0', '100', '1', '1');
	
	INSERT INTO `ecs_touch_shop_config` VALUES ('910', '8', 'sms_fenxiao', 'select', '1,0', '', '1', '1');

	INSERT INTO `ecs_touch_shop_config` VALUES ('909', '1', 'fenxiao', 'select', '1,0', '', '1', '1');
	